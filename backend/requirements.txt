# =============================================================
# LegalDiff Backend — Python Dependencies
# =============================================================

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9  # sync driver for Celery worker

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Task Queue
celery[redis]>=5.3.6
redis>=5.0.0

# Config
pyyaml>=6.0

# HTTP (for health checks)
httpx>=0.26.0

# Existing pipeline dependencies (used by worker)
# — these are already in ../requirements.txt
# The worker imports from src/* which needs: docling, FlagEmbedding,
# qdrant-client, jellyfish, numpy, scipy, etc.
