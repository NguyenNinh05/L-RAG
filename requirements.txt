# Web API Server
fastapi
uvicorn[standard]
python-multipart
aiohttp

# Document Ingestion (PDF & DOCX)
python-docx
pymupdf4llm

# =============================================================
# Phase 1: Legal RAG Ingestion Pipeline
# =============================================================

# Document Parsing (Primary engine)
docling                    # pip install docling

# Document Parsing (OCR Fallback)
marker-pdf                 # pip install marker-pdf

# Pydantic v2 (Data Modeling)
pydantic>=2.0

# Graph Database (Embedded — local, no server needed)
kuzu                       # pip install kuzu

# Vector Database (Persistent)
chromadb

# =============================================================
# Utilities
# =============================================================
numpy
requests
tenacity
tqdm
nest-asyncio

# =============================================================
# Phase 2: Comparison & Alignment Pipeline
# =============================================================

# Vector Database — multi-vector (dense + sparse, in-memory & persistent)
qdrant-client>=1.7.0

# String similarity (Jaro-Winkler) cho structural matching
jellyfish>=1.0.0

# Embedding Model — BGE-M3 (dense + sparse + colbert) via FlagEmbedding
# pip install FlagEmbedding
# Note: requires torch >= 2.0, CUDA optional
FlagEmbedding>=1.2.5
